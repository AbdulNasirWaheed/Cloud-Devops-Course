# Day-40: Commands Reference
**#100DaysOfDevOps | DevOps & Cloud Journey**

## Topic
Day-40: Cloud Services & Container Fundamentals

## Commands
```bash
$ sudo systemct] start docker
$ sudo systemct] status docker
https: //docs. docker. com
sudo systemct] start docker
sudo systemct] enable dacker
sudo usermd a6 docker $USER
#4 http://security ubuntu. com/ubuntu resolute-security InRelease
sudo apt remve docker.io dacker-doc docker-compose dacker-compose-v2 podman-dacker containerd runc -y
sudo apt update
sudo apt install ca-certificates curl gnupg -y
sudo install -m 0755 -d_/etc/apt/keyrings
curl -FsSL_https://download. docker.com/Tinux/ubuntu/apa | sudo gpg --dearmor -o /etc/apt/keyrings/docker. apa
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin -y
sudo systemct] start dacker
sudo usermad -a6 dacker $USER
sudo docker ps
sudo docker rm 17931fcda5f1
sudo docker logs webserver
sudo docker logs 17931fcda5f1
```

---
*Commands reference for Day-40.*
